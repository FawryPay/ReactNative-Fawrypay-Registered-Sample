require "json"

package = JSON.parse(File.read(File.join(__dir__, "package.json")))
folly_compiler_flags = '-DFOLLY_NO_CONFIG -DFOLLY_MOBILE=1 -DFOLLY_USE_LIBCPP=1 -Wno-comma -Wno-shorten-64-to-32'

Pod::Spec.new do |s|
  s.name         = "rn-fawry-pay-sdk"
  s.version      = package["version"]
  s.summary      = package["description"]
  s.homepage     = package["homepage"]
  s.license      = package["license"]
  s.authors      = package["author"]

  s.platforms    = { :ios => "12.1" }
  s.source       = { :git => "https://github.com/FawryPay/ReactNative-Fawrypay-Registered-Sample", :tag => "#{s.version}" }

  # s.source_files = "ios/**/*.{h,m,mm,swift}"
  s.source           = { :path => '.' }
  s.source_files = "ios/**/*.{h,m,mm,swift}" , "Classes/**/*"

  # s.static_framework = false
  # s.prefix_header_file = true
  # s.header_mappings_dir = '**'
  # s.module_map = true

  s.dependency "React-Core"

  # s.dependency 'Alamofire'
  # s.dependency 'SDWebImage'
  # s.dependency 'IQKeyboardManagerSwift'
  # s.dependency 'SwiftMessages'
  # s.dependency 'RxSwift'
  # s.dependency 'MBRadioButton'

  s.dependency 'FawryPaySDK-Registered-Testing', '0.1.16'

    s.app_spec do |app_spec|
    # app_spec.dependency 'FawryPaySDK'
    # app_spec.dependency 'Alamofire'
    # app_spec.dependency 'SDWebImage'
    # app_spec.dependency 'IQKeyboardManagerSwift'
    # app_spec.dependency 'SwiftMessages'
    # app_spec.dependency 'RxSwift'
    # app_spec.dependency 'MBRadioButton'
  end

  s.platform = :ios, '12.1'

  # s.pod_target_xcconfig = { 'DEFINES_MODULE' => 'YES', 'EXCLUDED_ARCHS[sdk=iphonesimulator*]' => 'arm64' }
  # s.user_target_xcconfig = { 'EXCLUDED_ARCHS[sdk=iphonesimulator*]' => 'arm64' }
  s.swift_version = '5.0'
  
  # s.pod_target_xcconfig = {'BUILD_LIBRARY_FOR_DISTRIBUTION' => 'YES'}
  # s.user_target_xcconfig = {'BUILD_LIBRARY_FOR_DISTRIBUTION' => 'YES'}

  # Don't install the dependencies when we run `pod install` in the old architecture.
  if ENV['RCT_NEW_ARCH_ENABLED'] == '1' then
    s.compiler_flags = folly_compiler_flags + " -DRCT_NEW_ARCH_ENABLED=1"
    s.pod_target_xcconfig    = {
        "HEADER_SEARCH_PATHS" => "\"$(PODS_ROOT)/boost\"",
        "OTHER_CPLUSPLUSFLAGS" => "-DFOLLY_NO_CONFIG -DFOLLY_MOBILE=1 -DFOLLY_USE_LIBCPP=1",
        "CLANG_CXX_LANGUAGE_STANDARD" => "c++17",
        "BUILD_LIBRARY_FOR_DISTRIBUTION" => "YES"
    }
    s.dependency "React-Codegen"
    s.dependency "RCT-Folly"
    s.dependency "RCTRequired"
    s.dependency "RCTTypeSafety"
    s.dependency "ReactCommon/turbomodule/core"
  end
end
